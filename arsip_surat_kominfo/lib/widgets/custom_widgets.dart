// lib/widgets/custom_widgets.dart

import 'package:flutter/material.dart';

class CustomWidgetExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Contoh Widget Kustom'),
    );
  }
}
